{
  "name": "@emdzej/swsrs-example-chat",
  "version": "0.0.0",
  "private": true,
  "description": "Two-party text chat over a swsrs relay session.",
  "type": "module",
  "bin": {
    "swsrs-chat": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "start": "node --enable-source-maps dist/index.js",
    "dev": "tsc && node --enable-source-maps dist/index.js"
  },
  "engines": {
    "node": ">=22.12"
  },
  "dependencies": {
    "@emdzej/swsrs-client": "workspace:*",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^7.0.2"
  }
}
